import React from 'react';
import Jobs from '../component/Jobs';
import Layout from '../component/Layout';

const jobs = () => {
    return (
        <>
             <Layout>
                <Jobs></Jobs>
             </Layout>
        </>
    );
};

export default jobs;